<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color del texto cuando el botón está seleccionado -->
    <item android:state_selected="true" android:color="@android:color/white" />
    <!-- Color del texto cuando el botón no está seleccionado -->
    <item android:color="@android:color/white" android:alpha="0.7"/>
</selector>